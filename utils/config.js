// Deprecated shim — use API_BASE / SERVER_URL from "@/lib/reerhub" instead.
// Kept so any stale import keeps working with a single source of truth.
export { SERVER_URL, API_BASE } from "@/lib/reerhub";
